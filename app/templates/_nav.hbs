<nav>
  <LinkTo @route="index" class="main">
    <img src={{model.logoUrl}} alt="Site logo">
  </LinkTo>
  <div class="menu">
    <Input @value={{tempQuery}} />
    <button {{action "navSearch" tempQuery}}>
      Search
    </button>
    <button {{action "watch"}}>
      Watch Live
    </button>
  </div>
</nav>