<div class="schedule-page">
  <div class="row">
    <div class="col-xs-12 col-lg-8">
      <h2 class="schedule-header">Schedule - {{moment-format currentDate 'LL'}}</h2>
    </div>
    <div class="col-xs-12 col-lg-4">
      <div class="schedule-date-picker">
					  <span class="picker glyphicon glyphicon-triangle-left" aria-hidden="true" {{action 'prevDay'}}></span>
						{{date-picker date=currentDay valueFormat='YYYY-MM-DD'}}
						<span class="picker glyphicon glyphicon-triangle-right" aria-hidden="true" {{action 'nextDay'}}></span>
          </div>
    </div>

  </div>
  <div class="schedule-wrapper clearfix">
		<div class="col-xs-12">
		{{#if media.isLg}}
			{{shows-schedule schedule=model class="shows-schedule is-desktop"}}
		{{else}}
			{{shows-schedule schedule=model class="shows-schedule is-mobile"}}
		{{/if}}
    </div>
  </div>
</div>
