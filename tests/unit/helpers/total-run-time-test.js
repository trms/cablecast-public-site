import {
  totalRunTime
} from '../../../helpers/total-run-time';
import { module, test } from 'qunit';

module('TotalRunTimeHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = totalRunTime(42);
  assert.ok(result);
});
