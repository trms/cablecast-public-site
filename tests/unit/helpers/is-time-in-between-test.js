import {
  isTimeInBetween
} from '../../../helpers/is-time-in-between';
import { module, test } from 'qunit';

module('IsTimeInBetweenHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = isTimeInBetween(42);
  assert.ok(result);
});
