import {
  momentFormat
} from '../../../helpers/moment-format';
import { module, test } from 'qunit';

module('MomentFormatHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = momentFormat(42);
  assert.ok(result);
});
