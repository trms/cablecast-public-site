import {
  embedCode
} from '../../../helpers/embed-code';
import { module, test } from 'qunit';

module('EmbedCodeHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = embedCode(42);
  assert.ok(result);
});
